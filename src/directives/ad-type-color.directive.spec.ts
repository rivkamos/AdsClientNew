import { AdTypeColorDirective } from './ad-type-color.directive';

describe('AdTypeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AdTypeColorDirective();
    expect(directive).toBeTruthy();
  });
});
